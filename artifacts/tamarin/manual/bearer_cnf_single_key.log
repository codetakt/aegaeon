maude tool: 'maude'
 checking version: 3.5.1. OK.
 checking installation: OK.
[Theory BearerCnfSingleKey] Theory loaded
[Theory BearerCnfSingleKey] Theory translated
[Theory BearerCnfSingleKey] Derivation checks started
[Theory BearerCnfSingleKey] Derivation checks ended
[Theory BearerCnfSingleKey] Theory closed
theory BearerCnfSingleKey begin

// Function signature and definition of the equational theory E

functions: fst/1, pair/2, snd/1
equations: fst(<x.1, x.2>) = x.1, snd(<x.1, x.2>) = x.2





rule (modulo E) Issue_Token_DPoP:
   [ Fr( ~token ), Fr( ~jkt ) ] --[ CnfDpop( ~token, ~jkt ) ]-> [ ]

  /* has exactly the trivial AC variant */

rule (modulo E) Issue_Token_mTLS:
   [ Fr( ~token ), Fr( ~x5t ) ] --[ CnfMtls( ~token, ~x5t ) ]-> [ ]

  /* has exactly the trivial AC variant */

lemma cnf_single_key:
  all-traces
  "¬(∃ token jkt x5t #i #j.
      (CnfDpop( token, jkt ) @ #i) ∧ (CnfMtls( token, x5t ) @ #j))"
/*
guarded formula characterizing all counter-examples:
"∃ token jkt x5t #i #j.
  (CnfDpop( token, jkt ) @ #i) ∧ (CnfMtls( token, x5t ) @ #j)"
*/
by simplify







/* All wellformedness checks were successful. */

/*
Generated from:
Tamarin version 1.10.0
Maude version 3.5.1
Git revision: UNKNOWN, branch: UNKNOWN
Compiled at: 2025-12-19 02:05:44.696222109 UTC
*/

end

==============================================================================
summary of summaries:

analyzed: ./bearer/cnf_single_key.spthy

  processing time: 0.21s

  cnf_single_key (all-traces): verified (1 steps)

==============================================================================
