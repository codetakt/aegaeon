pub(super) const SELECT_ENVIRONMENT_POLICY_SQL: &str = r"
SELECT
  pkce_required,
  dcr_enabled,
  dcr_everparse_runtime_enabled,
  require_state_parameter,
  strict_authorize_redirect,
  require_client_auth_token,
  require_client_auth_par,
  require_client_auth_introspection,
  require_client_auth_revocation,
  sender_constrained::text AS sender_constrained,
  require_scope_subset,
  require_audience_match,
  retain_refresh_chain,
  enforce_refresh_sender_binding,
  dpop_strict,
  dpop_iat_window_seconds,
  dpop_require_nonce,
  dpop_nonce_ttl_seconds,
  require_pushed_authorization_requests,
  par_expires_in_seconds,
  device_code_ttl_seconds,
  device_code_poll_interval_seconds,
  activation_token_default_ttl_seconds,
  password_reset_token_default_ttl_seconds,
  recovery_token_max_ttl_seconds,
  client_secret_default_expiration_days,
  client_secret_max_expiration_days,
  private_key_jwt_enabled,
  client_jwt_allowed_algs,
  client_jwt_require_kid,
  jwt_leeway_seconds,
  pkjwt_jti_window_seconds,
  jose_header_max_len,
  jwks_allow_kid_reuse,
  jwks_circuit_open_fails,
  jwks_circuit_reset_seconds,
  jwks_cache_ttl_seconds,
  jwks_cache_gc_interval_seconds,
  jwks_http_timeout_seconds,
  jwks_refresh_skew_seconds,
  jwks_shared_state_max_age_seconds,
  jwks_max_body_bytes,
  jwks_http_retries,
  jwt_bearer_allow_client_subject,
  jwt_bearer_jti_window_seconds,
  request_object_jti_ttl_seconds,
  request_object_everparse_runtime_enabled,
  jwt_access_tokens_enabled,
  jwt_introspection_enabled,
  jwt_introspection_exp_seconds,
  dcr_require_pkce_for_public,
  dcr_require_pkce_for_confidential,
  dcr_require_sender_constrained,
  dcr_allowed_sender_methods,
  ssa_jwt_pem,
  ssa_expected_iss,
  ssa_expected_aud,
  ssa_leeway_seconds,
  oidc_enabled,
  oidc_enable_discovery,
  oidc_enable_userinfo,
  oidc_enable_logout,
  oidc_enable_backchannel_logout,
  oidc_logout_session_ttl_seconds,
  oidc_backchannel_logout_timeout_seconds,
  oidc_require_nonce,
  mtls_enabled,
  mtls_base_url,
  mtls_alias_par_enabled,
  federation_outbound_allowed_domains,
  upstream_outbound_allowed_domains,
  federation_entity_cache_ttl_seconds,
  federation_trust_chain_cache_ttl_seconds,
  federation_cache_max_entries,
  crypto_profile,
  allowed_signing_algorithms,
  allowed_grant_types,
  access_token_time_to_live_seconds,
  id_token_time_to_live_seconds,
  refresh_token_time_to_live_seconds,
  authorization_code_time_to_live_seconds,
  stepup_challenge_ttl_seconds,
  upstream_auth_ttl_seconds,
  upstream_logout_relay_ttl_seconds,
  upstream_discovery_cache_ttl_seconds,
  upstream_jwks_cache_ttl_seconds,
  cleanup_interval_seconds,
  runtime_config_monitor_interval_seconds,
  auth_session_ttl_seconds,
  auth_max_sessions,
  authorization_details_types_supported,
  acr_values_supported,
  default_acr,
  local_password_acr,
  jwks_local_cache_max_entries,
  upstream_discovery_cache_max_entries,
  upstream_jwks_cache_max_entries
FROM aegaeon.environment_policies
WHERE environment_id = $1
";
